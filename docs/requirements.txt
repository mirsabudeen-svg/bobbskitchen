# BOBB Kitchen — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core agent framework ──────────────────
openai-agents>=0.0.7          # OpenAI Agents SDK (handoffs, tools, guardrails)
openai>=1.30.0                # OpenAI Python client (used by agents SDK)

# ── API server ────────────────────────────
fastapi>=0.111.0              # REST + WebSocket API
uvicorn[standard]>=0.29.0     # ASGI server
pydantic>=2.7.0               # Request/response models

# ── HTTP client (for ComfyUI, print API) ─
httpx>=0.27.0                 # Async HTTP

# ── WhatsApp (Twilio) ─────────────────────
twilio>=9.0.0                 # WhatsApp Business API

# ── Utilities ────────────────────────────
python-dotenv>=1.0.0          # Load .env config
python-multipart>=0.0.9       # Form data handling in FastAPI
