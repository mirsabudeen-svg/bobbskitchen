import { ScreenShell } from '../components/ScreenShell';

/** PRODUCT_SELECTION screen — Sprint 0 stub. */
export function ProductSelectionScreen() {
  return <ScreenShell stateName="PRODUCT_SELECTION" />;
}
